const FORMSPREE_ENDPOINT = 'https://formspree.io/f/xpwvjwjg';function getContactData(form) {const data = new FormData(form);return {name: String(data.get('name') || '').trim(),email: String(data.get('email') || '').trim(),message: String(data.get('message') || '').trim(),data,};}function hasRequiredContactData(contactData) {return Boolean(contactData.name && contactData.email && contactData.message);}function isValidEmail(email) {return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);}function submitContactForm(data) {return fetch(FORMSPREE_ENDPOINT, {method: 'POST',body: data,headers: { Accept: 'application/json' },});}function handleContactResponse(response) {if (response.ok)window.location.href = './send-mail.html';elsealert('Es gab ein Problem beim Senden deiner Nachricht.');}function handleContactError(error) {console.error(error);alert(`Ein Fehler ist aufgetreten: ${error}`);}function sendMail(event) {event.preventDefault();const contactData = getContactData(event.target);if (!hasRequiredContactData(contactData))return alert('Bitte fülle alle Felder aus, bevor du die Nachricht sendest.');if (!isValidEmail(contactData.email))return alert('Bitte gib eine gültige E-Mail-Adresse ein.');submitContactForm(contactData.data).then(handleContactResponse).catch(handleContactError);}

const ratingStars = document.querySelectorAll('#stars span');const ratingOutput = document.getElementById('rating_value');let selectedRating = 0;function highlightRatingStars(count) {ratingStars.forEach((star) => {const value = Number(star.dataset.value);star.classList.toggle('filled', value <= count);});}function selectRating(star) {selectedRating = Number(star.dataset.value);if (!ratingOutput)return;ratingOutput.textContent = `Bewertung: ${selectedRating} von 5 Sternen`;}function registerRatingStar(star) {star.addEventListener('mouseover', () => highlightRatingStars(Number(star.dataset.value)));star.addEventListener('mouseout', () => highlightRatingStars(selectedRating));star.addEventListener('click', () => selectRating(star));}ratingStars.forEach(registerRatingStar);

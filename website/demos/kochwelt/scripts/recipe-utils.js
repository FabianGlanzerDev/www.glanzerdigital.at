function getPortionCount(inputSelector, warningSelector) {const input = document.querySelector(inputSelector);const warning = document.querySelector(warningSelector);const portions = Number(input?.value);const isValid = portions >= 1 && portions <= 20 && !Number.isNaN(portions);if (warning)warning.style.display = isValid ? 'none' : 'inline';return isValid ? portions : null;}function formatIngredient(value, unit) {const formattedValue = value % 1 === 0 ? value.toFixed(0) : value.toFixed(1);return `${formattedValue} ${unit}`;}function setIngredientText(id, text) {const element = document.getElementById(id);if (element)element.textContent = text;}

function resetAddModalText() {modal.classList.remove("edit-mode");modalTitle.textContent = "Add contact";modalSubtitle.textContent = "Tasks are better with a team!";modalSubtitle.style.display = "block";}function resetAddAvatar() {contactPlaceholder.classList.remove("edit-mode");contactPlaceholder.style.background = "var(--form-input-border)";contactAvatarInitials.style.color = "";contactAvatarInitials.textContent = "";}function setAddButtons() {secondaryBtnText.textContent = "Cancel";secondaryBtnIcon.style.display = "block";submitBtnText.textContent = "Create contact";}function clearColorOptions() {if (colorOptions) {colorOptions.innerHTML = "";}}function setEditMode(contact) {modal.classList.add("edit-mode");setEditModalText();fillContactForm(contact);setEditAvatar(contact);setEditButtons();renderColorOptions();}function setEditModalText() {modalTitle.textContent = "Edit contact";modalSubtitle.style.display = "none";}function fillContactForm(contact) {document.getElementById("contactName").value = contact.name;document.getElementById("contactEmail").value = contact.email;document.getElementById("contactPhone").value = contact.phone || "";}function setEditAvatar(contact) {selectedContactColor = contact.color || contact.contactColor || "var(--profile-orange)";contactPlaceholder.classList.add("edit-mode");contactPlaceholder.style.background = selectedContactColor;contactAvatarInitials.style.color = getUserTextColor(selectedContactColor);contactAvatarInitials.textContent = getUserInitials(contact.name);}function setEditButtons() {secondaryBtnText.textContent = "Delete";secondaryBtnIcon.style.display = "none";submitBtnText.textContent = "Save";}function selectContactColor(color) {selectedContactColor = color;contactPlaceholder.style.background = color;contactAvatarInitials.style.color = getUserTextColor(color);renderColorOptions();closeColorDropdown();}function getRandomContactColor() {return getRandomProfileColor();}function renderContactDetails(contact) {const contactDetails = document.getElementById("contactDetails");const contactColor = getContactAvatarColor(contact);const contactView = getContactTemplateView(contact, contactColor);contactDetails.innerHTML = getContactsHeaderTemplate() + getContactDetailTemplate(contactView);}function getContactTemplateView(contact, contactColor) {return {name: escapeHtml(contact.name),displayName: escapeHtml(getContactDisplayName(contact)),email: escapeHtml(contact.email),phone: escapeHtml(contact.phone),initials: escapeHtml(getUserInitials(contact.name)),color: escapeHtml(contactColor),textColor: escapeHtml(getUserTextColor(contactColor))};}function renderContacts() {const contactsList = document.getElementById("contactsList");contactsList.innerHTML = "";let currentLetter = "";contacts.forEach(function (contact, index) {currentLetter = renderContactEntry(contactsList, contact, index, currentLetter);});}function renderContactEntry(contactsList, contact, index, currentLetter) {const firstLetter = contact.name.charAt(0).toUpperCase();if (firstLetter !== currentLetter) {contactsList.innerHTML += getContactGroupTemplate(escapeHtml(firstLetter));currentLetter = firstLetter;}contactsList.innerHTML += getRenderedContactCard(contact, index);return currentLetter;}function getRenderedContactCard(contact, index) {const activeClass = index === currentContactIndex ? "active" : "";const contactColor = getContactAvatarColor(contact);const contactView = getContactTemplateView(contact, contactColor);return getContactCardTemplate(contactView, index, activeClass);}function getContactAvatarColor(contact) {return contact.color || contact.contactColor || "var(--profile-orange)";}function renderColorOptions() {if (!colorOptions)return;colorOptions.innerHTML = "";profileColors.forEach(function (color) {colorOptions.innerHTML += getRenderedColorOption(color);});}function getRenderedColorOption(color) {const activeClass = color === selectedContactColor ? "active" : "";return getColorOptionTemplate(color, activeClass);}function renderEmptyContactDetails() {const contactDetails = document.getElementById("contactDetails");contactDetails.innerHTML = getContactsHeaderTemplate();}function setAddMode() {resetAddModalText();resetAddAvatar();setAddButtons();selectedContactColor = getRandomContactColor();clearColorOptions();closeColorDropdown();}function showContactToast(message) {if (!contactToast)return;contactToast.textContent = message;contactToast.classList.add("active");setTimeout(function () {contactToast.classList.remove("active");}, 2000);}function toggleColorDropdown(event) {event.stopPropagation();if (!colorPicker)return;colorPicker.classList.toggle("open");}function closeColorDropdown() {if (!colorPicker)return;colorPicker.classList.remove("open");}function closeColorDropdownOnOutsideClick(event) {if (!colorPicker || !contactPlaceholder)return;if (colorPicker.contains(event.target))return;if (contactPlaceholder.contains(event.target))return;closeColorDropdown();}function toggleMobileOptionsMenu(event) {event.stopPropagation();if (!mobileOptionsMenu)return;mobileOptionsMenu.classList.toggle("active");}function closeMobileOptionsMenu() {if (!mobileOptionsMenu)return;mobileOptionsMenu.classList.remove("active");}

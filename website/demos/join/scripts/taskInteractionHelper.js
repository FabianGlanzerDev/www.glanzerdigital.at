function initializeHorizontalDragScroll(container) {if (!container)return;if (container.dataset.dragScrollInitialized)return;container.dataset.dragScrollInitialized = "true";container.dragScrollState = { startX: 0, startScrollLeft: 0, didDrag: false, pointerId: null };container.addEventListener("pointerdown", (event) => startHorizontalDrag(event, container));container.addEventListener("pointermove", (event) => moveHorizontalDrag(event, container));container.addEventListener("pointerup", () => stopHorizontalDrag(container, true));container.addEventListener("pointercancel", () => stopHorizontalDrag(container, false));container.addEventListener("click", (event) => preventClickAfterDrag(event, container), true);}function startHorizontalDrag(event, element) {if (event.pointerType !== "mouse" || event.button !== 0)return;if (event.target.closest(".task-card"))return;const state = element.dragScrollState;state.startX = event.clientX;state.startScrollLeft = element.scrollLeft;state.didDrag = false;state.pointerId = event.pointerId;}function moveHorizontalDrag(event, element) {const state = element.dragScrollState;if (event.pointerId !== state.pointerId)return;if (!state.didDrag && Math.abs(event.clientX - state.startX) > 4)beginHorizontalDrag(event, element);if (!state.didDrag)return;element.scrollLeft = state.startScrollLeft - (event.clientX - state.startX);}function beginHorizontalDrag(event, element) {element.dragScrollState.didDrag = true;element.classList.add("is-dragging");element.setPointerCapture(event.pointerId);}function stopHorizontalDrag(element, keepDragState) {const state = element.dragScrollState;element.classList.remove("is-dragging");state.pointerId = null;if (!keepDragState)state.didDrag = false;elsesetTimeout(() => { state.didDrag = false; }, 0);}function preventClickAfterDrag(event, element) {if (!element.dragScrollState.didDrag)return;event.preventDefault();event.stopImmediatePropagation();}function initializePriorityKeyboard(group) {if (!group)return;const labels = group.querySelectorAll("label[for]");updatePriorityAriaState(group);labels.forEach((label) => {label.addEventListener("keydown", (event) => selectPriorityWithKeyboard(event, group));});group.querySelectorAll('input[type="radio"]').forEach((input) => {input.addEventListener("change", () => updatePriorityAriaState(group));});}function selectPriorityWithKeyboard(event, group) {if (event.key !== "Enter" && event.key !== " ")return;event.preventDefault();const input = event.currentTarget.control;input.checked = true;input.dispatchEvent(new Event("change", { bubbles: true }));}function updatePriorityAriaState(group) {group.querySelectorAll("label[for]").forEach((label) => {label.setAttribute("aria-checked", String(label.control.checked));});}

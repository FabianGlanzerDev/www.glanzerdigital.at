function initializeTaskContactDropdown(dialog, inputSelector, renderSelected) {const input = dialog.querySelector(inputSelector);const dropdown = input.closest(".dropdown-list");const toggle = () => toggleDialogDropdown({ currentTarget: input });const close = () => closeTaskContactDropdown(dropdown);const arrow = input.parentElement.querySelector(".input-icon");initializeKeyboardDropdown(input, toggle, close);connectTaskContactDropdown(input, arrow, dropdown, toggle);initializeTaskContactDropdownKeys(dropdown, close);connectTaskContactCheckboxes(dropdown, renderSelected);}function connectTaskContactDropdown(input, arrow, dropdown, toggle) {input.addEventListener("click", () => openTaskContactDropdown(dropdown));arrow?.addEventListener("click", (event) => {event.stopPropagation();toggle();});input.addEventListener("input", filterTaskContacts);}function connectTaskContactCheckboxes(dropdown, renderSelected) {dropdown.querySelectorAll(".contact-checkbox").forEach((box) => {box.addEventListener("change", renderSelected);});}function initializeTaskContactDropdownKeys(dropdown, close) {trackDropdownPointerDown(dropdown);dropdown.addEventListener("focusout", () => closeDropdownAfterFocusLeaves(dropdown, close));initializeContactDropdownKeys(dropdown, close);}function openTaskContactDropdown(dropdown) {dropdown.classList.add("open");dropdown.querySelector("input").setAttribute("aria-expanded", "true");}function filterTaskContacts(event) {const query = event.currentTarget.value.trim().toLowerCase();const dropdown = event.currentTarget.closest(".dropdown-list");openTaskContactDropdown(dropdown);dropdown.querySelectorAll(".contact-option").forEach((option) => {const name = option.querySelector("span:nth-child(2)").textContent.toLowerCase();option.style.display = name.startsWith(query) ? "" : "none";});}function closeTaskContactDropdown(dropdown) {closeOneDialogDropdown(dropdown);dropdown.querySelector("input").value = "";dropdown.querySelectorAll(".contact-option").forEach((option) => option.style.display = "");}function closeEditDropdownOnOutsideClick(event) {const dropdown = document.querySelector("#editTaskDialog .edit-assigned");if (!dropdown || dropdown.contains(event.target))return;closeTaskContactDropdown(dropdown);}

let contacts = [];let currentContactIndex = null;let modalMode = "add";let selectedContactColor = "var(--profile-orange)";const overlay = document.getElementById("addContactOverlay");const modal = document.querySelector(".add-contact-modal");const addContactBtn = document.getElementById("addContactBtn");const mobileAddContactBtn = document.getElementById("mobileAddContactBtn");const mobileMoreBtn = document.querySelector(".mobile-more-btn");const mobileBackBtn = document.querySelector(".mobile-back-btn");const closeContactModal = document.getElementById("closeContactModal");const cancelContactBtn = document.getElementById("cancelContactBtn");const addContactForm = document.getElementById("addContactForm");const contactsPage = document.querySelector(".contacts-page");const modalTitle = document.getElementById("contactModalTitle");const modalSubtitle = document.getElementById("contactModalSubtitle");const contactPlaceholder = document.getElementById("contactPlaceholder");const contactAvatarInitials = document.getElementById("contactAvatarInitials");const secondaryBtnText = document.getElementById("secondaryBtnText");const secondaryBtnIcon = document.getElementById("secondaryBtnIcon");const submitBtnText = document.getElementById("submitBtnText");const colorPicker = document.getElementById("colorPicker");const colorOptions = document.getElementById("colorOptions");const mobileOptionsMenu = document.getElementById("mobileOptionsMenu");const mobileEditContactBtn = document.getElementById("mobileEditContactBtn");const mobileDeleteContactBtn = document.getElementById("mobileDeleteContactBtn");const contactToast = document.getElementById("contactToast");const contactDetailsContainer = document.getElementById("contactDetails");const contactsListContainer = document.getElementById("contactsList");if (addContactBtn)addContactBtn.addEventListener("click", openAddContactOverlay);if (mobileAddContactBtn)mobileAddContactBtn.addEventListener("click", openAddContactOverlay);if (mobileMoreBtn)mobileMoreBtn.addEventListener("click", toggleMobileOptionsMenu);if (mobileBackBtn)mobileBackBtn.addEventListener("click", closeMobileContactView);if (closeContactModal)closeContactModal.addEventListener("click", closeAddContactOverlay);if (cancelContactBtn)cancelContactBtn.addEventListener("click", handleSecondaryButton);if (addContactForm)addContactForm.addEventListener("submit", handleContactSubmit);if (contactPlaceholder)contactPlaceholder.addEventListener("click", toggleColorDropdown);if (contactDetailsContainer)contactDetailsContainer.addEventListener("keydown", handleContactDetailTab);if (contactsListContainer)contactsListContainer.addEventListener("keydown", handleActiveContactTab);if (mobileEditContactBtn) {mobileEditContactBtn.addEventListener("click", function () {closeMobileOptionsMenu();openEditContactOverlay();});}if (mobileDeleteContactBtn) {mobileDeleteContactBtn.addEventListener("click", function () {closeMobileOptionsMenu();deleteContact();});}if (overlay) {overlay.addEventListener("pointerup", function (event) {if (event.target === overlay) {closeAddContactOverlay();}});}if (modal) {modal.addEventListener("click", function (event) {closeColorDropdownOnOutsideClick(event);event.stopPropagation();});}document.addEventListener("click", function (event) {closeColorDropdownOnOutsideClick(event);if (!mobileOptionsMenu || !mobileMoreBtn)return;const clickedInsideMenu = mobileOptionsMenu.contains(event.target);const clickedMoreButton = mobileMoreBtn.contains(event.target);if (!clickedInsideMenu && !clickedMoreButton) {closeMobileOptionsMenu();}});function openAddContactOverlay() {modalMode = "add";closeMobileOptionsMenu();closeColorDropdown();setAddMode();addContactForm.reset();addContactForm.classList.remove("submitted");overlay.classList.add("active");activateModal(overlay, document.getElementById("username"));}function openEditContactOverlay() {if (currentContactIndex === null)return;const contact = contacts[currentContactIndex];if (!contact)return;modalMode = "edit";closeMobileOptionsMenu();closeColorDropdown();setEditMode(contact);overlay.classList.add("active");activateModal(overlay, document.getElementById("username"));}function closeAddContactOverlay() {deactivateModal(overlay);overlay.classList.remove("active");closeColorDropdown();}function closeContactModalOnEscape(event) {if (event.key === "Escape" && overlay.classList.contains("active")) {closeAddContactOverlay();}}document.addEventListener("keydown", closeContactModalOnEscape);async function loadContacts() {const data = await getContactsData();contacts = createContactsArray(data);sortContactsByName();renderContacts();renderInitialContactDetails();}async function getContactsData() {const response = await fetch(getContactsUrl());ensureSuccessfulResponse(response, "Contacts could not be loaded.");return await response.json();}function getContactsUrl(contactId = "") {const path = contactId ? "/" + contactId : "";return getScopedDatabaseUrl("contacts" + path);}function createContactsArray(data) {if (!data)return [];return Object.entries(data).map(function ([id, contact]) {return { id: id, ...contact };});}function sortContactsByName() {contacts.sort(function (a, b) {return a.name.localeCompare(b.name);});}function renderInitialContactDetails() {if (currentContactIndex === null) {renderEmptyContactDetails();}}function showContact(index) {const contact = contacts[index];if (!contact)return;currentContactIndex = index;renderContacts();renderContactDetails(contact);document.querySelector(".contact-detail-name-box h2").focus({ preventScroll: true });openMobileContactDetails();}function handleContactDetailTab(event) {if (event.key !== "Tab" || isTouchNavigationLayout())return;if (event.shiftKey && event.target.closest(".edit-contact-btn")) {event.preventDefault();focusContactCard(currentContactIndex);return;}if (!event.shiftKey && event.target.closest(".contact-email")) {focusNextContactCard(event);}}function handleActiveContactTab(event) {if (event.key !== "Tab" || event.shiftKey || isTouchNavigationLayout())return;if (!event.target.matches(".contact-card.active"))return;const editButton = contactDetailsContainer.querySelector(".edit-contact-btn");if (!editButton)return;event.preventDefault();editButton.focus();}function focusContactCard(index) {document.querySelector(`.contact-card[data-contact-index="${index}"]`)?.focus();}function focusNextContactCard(event) {const nextCard = document.querySelector(`.contact-card[data-contact-index="${currentContactIndex + 1}"]`);if (!nextCard)return;event.preventDefault();nextCard.focus();}function openMobileContactDetails() {if (isTouchNavigationLayout()) {contactsPage.classList.add("mobile-detail-open");}}function isTouchNavigationLayout() {return window.matchMedia("(max-width: 1024px) and (hover: none) and (pointer: coarse)").matches;}function closeMobileContactView() {contactsPage.classList.remove("mobile-detail-open");closeMobileOptionsMenu();closeColorDropdown();}function handleContactSubmit(event) {event.preventDefault();addContactForm.classList.add("submitted");if (!validateContactForm())return;if (modalMode === "edit") {saveContact();return;}createContact();}function closeContactInterfaces() {closeMobileOptionsMenu();closeAddContactOverlay();closeMobileContactView();closeColorDropdown();}function handleSecondaryButton() {if (modalMode === "edit") {deleteContact();return;}closeAddContactOverlay();}loadContacts();

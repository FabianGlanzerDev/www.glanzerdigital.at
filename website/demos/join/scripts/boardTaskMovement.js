let draggedTaskId = null;let currentDropZone = null;let dropIndicatorElement = null;function setupBoardDragAndDrop() {const boardColumnsElement = document.querySelector('.board-columns');if (!boardColumnsElement) {return;}boardColumnsElement.addEventListener("dragstart", handleDragStart);boardColumnsElement.addEventListener("dragover", handleDragOver);boardColumnsElement.addEventListener("drop", handleDrop);boardColumnsElement.addEventListener("dragend", handleDragEnd);}function handleDragStart(event) {const taskCard = event.target.closest('.task-card');if (taskCard) {draggedTaskId = taskCard.dataset.taskId;taskCard.classList.add('dragging');}}function handleDragEnd(event) {const taskCard = event.target.closest(".task-card");if (taskCard)taskCard.classList.remove("dragging");finishTaskDrag();}function finishTaskDrag() {clearCurrentDropZone();removeDropIndicator();draggedTaskId = null;}function clearCurrentDropZone() {if (!currentDropZone)return;currentDropZone.classList.remove("drag-over");currentDropZone = null;}function getCardsInList(taskList) {return [...taskList.querySelectorAll('.task-card:not(.dragging)')];}function getDropTargetCard(taskList, clientX, clientY) {const isRow = getComputedStyle(taskList).flexDirection === 'row';return getCardsInList(taskList).find((card) => {const rect = card.getBoundingClientRect();return isRow? clientX < rect.left + rect.width / 2: clientY < rect.top + rect.height / 2;}) || null;}function getDropIndex(taskList, targetCard) {const cards = getCardsInList(taskList);return targetCard ? cards.indexOf(targetCard) : cards.length;}function getOrCreateDropIndicator() {if (!dropIndicatorElement) {dropIndicatorElement = document.createElement("div");dropIndicatorElement.className = "drop-indicator";}return dropIndicatorElement;}function removeDropIndicator() {if (dropIndicatorElement && dropIndicatorElement.parentElement) {dropIndicatorElement.remove();}}function handleDragOver(event) {event.preventDefault();const taskList = event.target.closest('.task-list');updateCurrentDropZone(taskList);if (!taskList) {removeDropIndicator();return;}positionDropIndicator(taskList, event.clientX, event.clientY);}function updateCurrentDropZone(taskList) {if (taskList === currentDropZone)return;clearCurrentDropZone();if (taskList)taskList.classList.add("drag-over");currentDropZone = taskList;}function positionDropIndicator(taskList, clientX, clientY) {const targetCard = getDropTargetCard(taskList, clientX, clientY);taskList.insertBefore(getOrCreateDropIndicator(), targetCard);}function handleDrop(event) {const taskList = event.target.closest('.task-list');if (!taskList || !draggedTaskId)return;moveDroppedTask(taskList, event.clientX, event.clientY);finishTaskDrag();}function moveDroppedTask(taskList, clientX, clientY) {const column = Object.keys(boardColumns).find((key) => boardColumns[key].elementId === taskList.id);if (!column)return;const targetCard = getDropTargetCard(taskList, clientX, clientY);moveTaskToPosition(draggedTaskId, column, getDropIndex(taskList, targetCard));}function setupMobileTaskMoveEvents() {const boardColumnsElement = document.querySelector(".board-columns");if (!boardColumnsElement) {return;}boardColumnsElement.addEventListener("click", handleMobileTaskMoveClick);document.addEventListener("click", closeMobileMoveMenu);}function handleMobileTaskMoveClick(event) {const moveOption = event.target.closest(".mobile-move-option");const moveButton = event.target.closest(".mobile-move-task-btn");if (moveOption) {return handleMobileMoveOptionClick(event, moveOption);}if (moveButton) {return handleMobileMoveButtonClick(event, moveButton);}}function handleMobileMoveButtonClick(event, moveButton) {event.stopImmediatePropagation();toggleMobileMoveMenu(moveButton);}function handleMobileMoveOptionClick(event, moveOption) {event.stopImmediatePropagation();moveTaskMobile(moveOption.dataset.taskId, moveOption.dataset.column);}function toggleMobileMoveMenu(moveButton) {const taskCard = moveButton.closest(".task-card");const isOpen = taskCard.querySelector(".mobile-move-menu");const taskId = moveButton.dataset.taskId;const currentColumn = moveButton.dataset.column;closeMobileMoveMenu();if (!isOpen) {taskCard.appendChild(createMobileMoveMenu(taskId, currentColumn));}}function createMobileMoveMenu(taskId, currentColumn) {const menu = document.createElement("div");const options = Object.entries(boardColumns).filter(([column]) => column !== currentColumn).map(([column, data]) => getMobileMoveOptionTemplate(taskId, column, data.label)).join("");menu.className = "mobile-move-menu";menu.innerHTML = getMobileMoveMenuTemplate(options);return menu;}async function moveTaskMobile(taskId, column) {if (!boardColumns[column]) {return;}const endIndex = getSortedColumnTasks(column).filter((task) => task.id !== taskId).length;await moveTaskToPosition(taskId, column, endIndex);}async function moveTaskToPosition(taskId, targetColumn, targetIndex) {if (!boardColumns[targetColumn])return;const task = boardTasks.find((task) => task.id === taskId);if (!task)return;task.column = targetColumn;const targetTasks = getReorderedColumnTasks(task, targetColumn, targetIndex);await persistColumnOrder(targetColumn, targetTasks, taskId);renderBoardTasks(boardTasks, boardContacts);showBoardStatusMessage(`Task moved to ${boardColumns[targetColumn].label}.`);}function getReorderedColumnTasks(task, column, targetIndex) {const tasks = getSortedColumnTasks(column).filter((entry) => entry.id !== task.id);tasks.splice(targetIndex, 0, task);tasks.forEach((entry, index) => { entry.order = index; });return tasks;}function getSortedColumnTasks(column) {return boardTasks.filter((task) => task.column === column).sort((a, b) => (a.order ?? 0) - (b.order ?? 0));}async function persistColumnOrder(column, tasks, movedTaskId) {const updates = {};tasks.forEach((task) => { updates[`${task.id}/order`] = task.order; });updates[`${movedTaskId}/column`] = column;const response = await fetch(getTasksUrl(), {method: "PATCH",headers: { "Content-Type": "application/json" },body: JSON.stringify(updates)});ensureSuccessfulResponse(response, "Task position could not be saved.");}function closeMobileMoveMenu() {const openMenu = document.querySelector(".mobile-move-menu");if (openMenu) {openMenu.remove();}}setupBoardDragAndDrop();setupMobileTaskMoveEvents();

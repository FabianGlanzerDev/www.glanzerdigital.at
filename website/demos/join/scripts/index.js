function hasInvalidLoginInput(email, password) {const invalid = !email.value.trim() || !password.value.trim();email.classList.toggle('error', invalid);password.classList.toggle('error', invalid);return invalid;}async function getUserProfile(uid) {const response = await fetch(getUserDatabaseUrl(uid));ensureSuccessfulResponse(response, "User profile could not be loaded.");const user = await response.json();return { id: uid, user };}async function checkLogin() {const email = document.getElementById('email');const password = document.getElementById('password');if (hasInvalidLoginInput(email, password))return showLoginError('Check your email and password. Please try again.');try {completeUserLogin(await authenticateUser(email.value.trim(), password.value.trim()));}catch (error) {showLoginError('Check your email and password. Please try again.');}}async function authenticateUser(email, password) {const credential = await signInWithEmailAndPassword(firebaseAuth, email, password);return getUserProfile(credential.user.uid);}function completeUserLogin(userEntry) {setActiveUser(userEntry.id, userEntry.user);startActiveSession(userEntry.id, "user");sessionStorage.setItem('joinShowMobileGreeting', 'true');window.location.href = './subpages/summary.html';}async function loginAsGuest() {try {setGuestButtonDisabled(true);clearError();await completeGuestLogin();}catch (error) {showLoginError('Guest login is currently not available. Please try again.');}finally {setGuestButtonDisabled(false);}}function setGuestButtonDisabled(isDisabled) {const guestButton = document.querySelector('.guest-btn');guestButton.disabled = isDisabled;}async function completeGuestLogin() {const guestUser = await resetGuestSandbox();setActiveUser(guestUserId, guestUser);startActiveSession(guestUserId, "guest");sessionStorage.setItem('joinShowMobileGreeting', 'true');window.location.href = './subpages/summary.html';}async function resetGuestSandbox() {const guestUser = await getGuestUser();guestUser.contacts = await getGlobalData("contacts");guestUser.tasks = await getGlobalData("tasks");await saveGuestSandbox(guestUser);return guestUser;}async function getGuestUser() {const response = await fetch(getUserDatabaseUrl(guestUserId));ensureSuccessfulResponse(response, "Guest profile could not be loaded.");const guestUser = await response.json();return guestUser || getDefaultGuestUser();}function getDefaultGuestUser() {return {email: 'guest@login.com',name: 'Guest',password: 'guest123!',userColor: 'var(--profile-blue)'};}async function getGlobalData(path) {const response = await fetch(getDatabaseUrl(path));ensureSuccessfulResponse(response, "Example data could not be loaded.");const data = await response.json();return data || {};}async function saveGuestSandbox(guestUser) {const response = await fetch(getUserDatabaseUrl(guestUserId), {method: "PUT",headers: { "Content-Type": "application/json" },body: JSON.stringify(guestUser)});ensureSuccessfulResponse(response, "Guest sandbox could not be prepared.");}function showLoginError(message) {document.getElementById('email').classList.add('error');document.getElementById('password').classList.add('error');const errorMsg = document.getElementById('errorMessage');errorMsg.textContent = message;errorMsg.classList.add('show');}function togglePasswordIcon() {const password = document.getElementById('password');const icon = document.getElementById('toggleIcon');const toggleButton = document.getElementById('togglePasswordButton');const visible = password.type === 'text';const hasPassword = password.value.length > 0;toggleButton.setAttribute('aria-label', visible ? 'Hide password' : 'Show password');toggleButton.disabled = !hasPassword;if (!hasPassword)return icon.src = './assets/icons/lock.webp';icon.src = visible ? './assets/icons/visability.webp' : './assets/icons/visabilityOff.webp';}document.getElementById('password').addEventListener('input', togglePasswordIcon);document.getElementById('loginForm').addEventListener('submit', function (event) {event.preventDefault();checkLogin();});document.getElementById('togglePasswordButton').addEventListener('click', () => {const password = document.getElementById('password');if (!password.value.length)return;password.type = password.type === 'password' ? 'text' : 'password';togglePasswordIcon();});document.getElementById('email').addEventListener('focus', clearError);document.getElementById('password').addEventListener('focus', clearError);document.querySelector('.guest-btn').addEventListener('click', loginAsGuest);function clearError() {document.getElementById('email').classList.remove('error');document.getElementById('password').classList.remove('error');const errorMsg = document.getElementById('errorMessage');errorMsg.classList.remove('show');errorMsg.textContent = '';}const introLayer = document.querySelector('.intro-layer');function finishLoginIntro() {document.body.classList.remove('intro-running');introLayer?.remove();}if (!introLayer || window.matchMedia('(prefers-reduced-motion: reduce)').matches) {finishLoginIntro();}else {introLayer.addEventListener('animationend', finishLoginIntro, { once: true });}

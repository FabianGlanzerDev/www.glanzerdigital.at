const mobileSummaryMediaQuery = window.matchMedia("(max-width: 1024px) and (hover: none) and (pointer: coarse)");const mobileGreetingStorageKey = "joinShowMobileGreeting";async function renderSummaryMetrics() {const tasks = await getSummaryTasks();const urgentTasks = tasks.filter((task) => task.priority === "urgent");setSummaryValue("summaryToDo", countTasksByColumn(tasks, "todo"));setSummaryValue("summaryDone", countTasksByColumn(tasks, "done"));setSummaryValue("summaryUrgent", urgentTasks.length);setSummaryValue("summaryUpcomingDeadline", getUpcomingDeadlineText(urgentTasks));setSummaryValue("summaryTasksInBoard", tasks.length);setSummaryValue("summaryTasksInProgress", countTasksByColumn(tasks, "inprogress"));setSummaryValue("summaryAwaitingFeedback", countTasksByColumn(tasks, "awaitfeedback"));}async function getSummaryTasks() {try {return await getTasks();}catch (error) {return [];}}function countTasksByColumn(tasks, column) {return tasks.filter((task) => task.column === column).length;}function getUpcomingDeadlineText(tasks) {const upcomingDeadline = tasks.map((task) => task.dueDate).filter(Boolean).sort()[0];return upcomingDeadline ? formatSummaryDate(upcomingDeadline) : "No deadline";}function formatSummaryDate(dateString) {return new Date(dateString + "T00:00:00").toLocaleDateString("en-US", {year: "numeric",month: "long",day: "2-digit"});}function setSummaryValue(elementId, value) {const element = document.getElementById(elementId);if (!element)return;element.textContent = value;}function getTimeBasedGreeting() {const currentHour = new Date().getHours();if (currentHour < 12)return "Good morning";if (currentHour < 18)return "Good afternoon";return "Good evening";}function renderActiveUser() {const greetingElement = document.querySelector(".summary-user h2");const activeUserElement = document.getElementById("activeUser");const activeUser = getActiveUser();if (!greetingElement || !activeUserElement || !activeUser)return;const isGuest = activeUser.id === guestUserId;const greeting = getTimeBasedGreeting();greetingElement.textContent = isGuest ? `${greeting}!` : `${greeting},`;activeUserElement.textContent = isGuest ? "" : activeUser.name;activeUserElement.style.color = "var(--text-user)";}function getSummarySection() {return document.querySelector(".summary-section");}function hideMobileGreeting(summarySection) {summarySection.classList.remove("mobile-greeting-active");summarySection.classList.add("mobile-greeting-finished");}function showMobileGreeting() {const summarySection = getSummarySection();const shouldShowGreeting = sessionStorage.getItem(mobileGreetingStorageKey) === "true";sessionStorage.removeItem(mobileGreetingStorageKey);if (!summarySection || !mobileSummaryMediaQuery.matches || !shouldShowGreeting || prefersReducedMotion()) {return;}summarySection.classList.add("mobile-greeting-active");addMobileGreetingEndListener(summarySection);}function prefersReducedMotion() {return window.matchMedia("(prefers-reduced-motion: reduce)").matches;}function addMobileGreetingEndListener(summarySection) {const greeting = summarySection.querySelector(".summary-user");if (!greeting)return hideMobileGreeting(summarySection);greeting.addEventListener("animationend", () => hideMobileGreeting(summarySection), { once: true });}function handleSummaryViewportChange(event) {const summarySection = getSummarySection();if (!summarySection) {return;}if (!event.matches)hideMobileGreeting(summarySection);}showMobileGreeting();renderActiveUser();renderSummaryMetrics();mobileSummaryMediaQuery.addEventListener("change", handleSummaryViewportChange);

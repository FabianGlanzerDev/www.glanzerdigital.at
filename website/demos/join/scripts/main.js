const BASE_URL = "https://join-issue-collector-9a6e1-default-rtdb.europe-west1.firebasedatabase.app/"; const activeUserStorageKey = "joinActiveUser"; const activeSessionStorageKey = "joinActiveSession"; const guestUserId = "guest"; const profileColors = ["var(--profile-orange)", "var(--profile-pink)", "var(--profile-violet)", "var(--profile-purple)", "var(--profile-cyan)", "var(--profile-turquoise)", "var(--profile-salmon)", "var(--profile-peach)", "var(--profile-magenta)", "var(--profile-gold)", "var(--profile-blue)", "var(--profile-lime)", "var(--profile-yellow)", "var(--profile-red)", "var(--profile-amber)"]; const brightProfileColors = ["var(--profile-yellow)", "var(--profile-lime)", "var(--profile-gold)", "var(--profile-cyan)", "var(--profile-turquoise)", "var(--profile-peach)", "var(--profile-amber)"]; function getDatabaseUrl(path) { let normalizedPath = path || ""; if (normalizedPath.startsWith("/")) { normalizedPath = normalizedPath.slice(1); } if (normalizedPath.endsWith(".json")) { normalizedPath = normalizedPath.slice(0, -5); } return BASE_URL + normalizedPath + ".json"; } function getUserDatabaseUrl(userId, path = "") { const cleanPath = path ? "/" + path.replace(/^\/+/, "") : ""; return getDatabaseUrl("users/" + userId + cleanPath); } function getCurrentUserId() { return getActiveUserId(); } function getScopedDatabaseUrl(path) { const currentUserId = getCurrentUserId(); if (!currentUserId) { throw new Error("Authentication is required."); } if (currentUserId === guestUserId) { return getUserDatabaseUrl(guestUserId, path); } return getDatabaseUrl(path); } function setActiveUser(userId, user) { const activeUser = { id: userId, name: user.name || userId, email: user.email || "", userColor: user.userColor || "blue" }; localStorage.setItem(activeUserStorageKey, JSON.stringify(activeUser)); } function startActiveSession(userId, loginType) { const activeSession = { userId, loginType }; sessionStorage.setItem(activeSessionStorageKey, JSON.stringify(activeSession)); } function hasValidActiveSession(activeUser) { const storedSession = sessionStorage.getItem(activeSessionStorageKey); if (!activeUser || !storedSession) return false; try { const activeSession = JSON.parse(storedSession); const hasValidLoginType = activeSession.loginType === "user" || activeSession.loginType === "guest"; const hasMatchingUser = activeSession.userId === activeUser.id; const hasMatchingGuestType = (activeUser.id === guestUserId) === (activeSession.loginType === "guest"); return hasValidLoginType && hasMatchingUser && hasMatchingGuestType; } catch (error) { sessionStorage.removeItem(activeSessionStorageKey); return false; } } function getActiveUser() { const activeUser = localStorage.getItem(activeUserStorageKey); if (!activeUser) return null; try { const parsedUser = JSON.parse(activeUser); return hasValidActiveSession(parsedUser) ? parsedUser : null; } catch (error) { clearActiveUser(); return null; } } function clearActiveUser() { localStorage.removeItem(activeUserStorageKey); sessionStorage.removeItem(activeSessionStorageKey); } function getActiveUserId() { const activeUser = getActiveUser(); return activeUser ? activeUser.id : null; } function normalizeText(value) { return String(value || "").trim().toLowerCase(); } function escapeHtml(value) { return String(value ?? "").replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#039;"); } function ensureSuccessfulResponse(response, message) { if (!response.ok) throw new Error(message); return response; } function isCurrentUserContact(contact) { const activeUser = getActiveUser(); if (!activeUser || !contact) return false; if (!activeUser.email || !contact.email) return false; return normalizeText(contact.email) === normalizeText(activeUser.email); } function getContactDisplayName(contact) { if (!contact) return ""; if (isCurrentUserContact(contact)) { return `${contact.name} (You)`; } return contact.name; } async function updateActiveUserAfterContactEdit(oldContact, updatedContact) { const activeUser = getActiveUser(); if (!activeUser || !isCurrentUserContact(oldContact)) return; const updatedUser = { ...activeUser, name: updatedContact.name || activeUser.name, email: updatedContact.email || activeUser.email, userColor: updatedContact.color || activeUser.userColor }; const userData = { name: updatedUser.name, email: updatedUser.email, userColor: updatedUser.userColor }; const response = await fetch(getUserDatabaseUrl(activeUser.id), { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(userData) }); ensureSuccessfulResponse(response, "User profile could not be updated."); setActiveUser(activeUser.id, updatedUser); } function getUserInitials(name) { return (name || "Guest").trim().split(" ").filter(Boolean).map((word) => word[0]).join("").slice(0, 2).toUpperCase(); } function getUserColor(userColor) { if (!userColor) return "var(--profile-blue)"; if (userColor.startsWith("var(") || userColor.startsWith("#") || userColor.startsWith("rgb")) return userColor; return `var(--profile-${userColor})`; } function getRandomProfileColor() { const randomIndex = Math.floor(Math.random() * profileColors.length); return profileColors[randomIndex]; } function getUserTextColor(userColor) { const avatarColor = getUserColor(userColor); return brightProfileColors.includes(avatarColor) ? "var(--text-primary)" : "var(--text-white)"; }

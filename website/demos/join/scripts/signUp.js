function togglePasswordIcon(inputId, iconId) {const input = document.getElementById(inputId);const icon = document.getElementById(iconId);const toggleButton = icon.closest('button');const visible = input.type === 'text';const hasPassword = input.value.length > 0;toggleButton.setAttribute('aria-label', visible ? 'Hide password' : 'Show password');toggleButton.disabled = !hasPassword;if (!hasPassword)return icon.src = '../assets/icons/lock.webp';icon.src = visible ? '../assets/icons/visability.webp' : '../assets/icons/visabilityOff.webp';}function togglePasswordVisibility(inputId, iconId) {const input = document.getElementById(inputId);if (!input.value.length)return;input.type = input.type === 'password' ? 'text' : 'password';togglePasswordIcon(inputId, iconId);}document.getElementById('password').addEventListener('input', () => togglePasswordIcon('password', 'passwordIcon'));document.getElementById('passwordToggle').addEventListener('click', () => togglePasswordVisibility('password', 'passwordIcon'));document.getElementById('confirmPassword').addEventListener('input', () => togglePasswordIcon('confirmPassword', 'confirmPasswordIcon'));document.getElementById('confirmPasswordToggle').addEventListener('click', () => togglePasswordVisibility('confirmPassword', 'confirmPasswordIcon'));function updateSignupButtonState() {const requiredFields = ['username', 'email', 'password', 'confirmPassword'];const termsCheckbox = document.getElementById('termsCheckbox');const signupBtn = document.getElementById('signupBtn');const allFieldsFilled = requiredFields.every((id) => {return document.getElementById(id).value.trim() !== '';});signupBtn.disabled = !allFieldsFilled || !termsCheckbox.checked;}['username', 'email', 'password', 'confirmPassword'].forEach((id) => {document.getElementById(id).addEventListener('input', updateSignupButtonState);});document.getElementById('termsCheckbox').addEventListener('change', updateSignupButtonState);updateSignupButtonState();function showValidationError(input, message, focusInput = true) {input.classList.add('error');const messageField = document.getElementById(`${input.id}Error`) || document.getElementById('errorMessage');messageField.textContent = message;if (focusInput)input.focus();return false;}function clearSignupErrors() {document.querySelectorAll('.signup-input-field input').forEach((input) => {input.classList.remove('error');});document.getElementById('errorMessage').textContent = '';}function clearFieldError(input) {input.classList.remove('error');const messageField = document.getElementById(`${input.id}Error`);if (messageField)messageField.textContent = '';}function getSignupFields() {return {name: document.getElementById('username'),email: document.getElementById('email'),password: document.getElementById('password'),confirmPassword: document.getElementById('confirmPassword'),terms: document.getElementById('termsCheckbox')};}function validateSignupForm() {const fields = getSignupFields();clearSignupErrors();return validateRequiredSignupFields(fields)&& validateSignupName(fields.name)&& validateSignupEmail(fields.email)&& validateSignupPasswordLength(fields.password)&& validateSignupPasswordsMatch(fields)&& validateSignupTerms(fields.terms);}function validateRequiredSignupFields(fields) {if (!fields.name.value.trim())return showValidationError(fields.name, 'Please enter your name.');if (!fields.email.value.trim())return showValidationError(fields.email, 'Please enter your email address.');if (!fields.password.value.trim())return showValidationError(fields.password, 'Please enter a password.');if (!fields.confirmPassword.value.trim())return showValidationError(fields.confirmPassword, 'Please confirm your password.');return true;}function validateSignupName(name, focusInput = true) {if (name.value.trim().length >= 3)return true;return showValidationError(name, 'Your name needs at least 3 characters.', focusInput);}function validateSignupEmail(email, focusInput = true) {const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;if (emailPattern.test(email.value.trim()))return true;return showValidationError(email, 'Please enter a valid email address.', focusInput);}function validateSignupPasswordLength(password, focusInput = true) {if (password.value.trim().length >= 6)return true;return showValidationError(password, 'Your password needs at least 6 characters.', focusInput);}function validateSignupPasswordsMatch(fields, focusInput = true) {if (fields.password.value === fields.confirmPassword.value)return true;return showValidationError(fields.confirmPassword, 'Your passwords do not match.', focusInput);}function validateSignupTerms(terms) {if (terms.checked)return true;return showValidationError(terms, 'Please accept the privacy policy.');}function validateFieldOnBlur(fieldId, validateFn) {const input = document.getElementById(fieldId);if (!input.value.trim())return;validateFn(input);}document.getElementById('username').addEventListener('blur', () => {validateFieldOnBlur('username', (input) => validateSignupName(input, false));});document.getElementById('email').addEventListener('blur', () => {validateFieldOnBlur('email', (input) => validateSignupEmail(input, false));});document.getElementById('password').addEventListener('blur', () => {validateFieldOnBlur('password', (input) => validateSignupPasswordLength(input, false));});document.getElementById('confirmPassword').addEventListener('blur', () => {validateFieldOnBlur('confirmPassword', () => validateSignupPasswordsMatch(getSignupFields(), false));});document.getElementById('signupForm').addEventListener('submit', (event) => {event.preventDefault();handleSignup();});document.querySelectorAll('.signup-input-field input').forEach((input) => {input.addEventListener('input', () => clearFieldError(input));});async function handleSignup() {if (!validateSignupForm())return;const signupBtn = document.getElementById('signupBtn');signupBtn.disabled = true;await createSignupAccount(signupBtn);}async function createSignupAccount(signupBtn) {try {const userCredential = await registerFirebaseUser();await saveSignupData(userCredential.user.uid);showSignupToast();}catch (error) {showSignupError(getSignupErrorMessage(error));}finally {updateSignupButtonState();}}function registerFirebaseUser() {const email = document.getElementById('email').value.trim();const password = document.getElementById('password').value.trim();return createUserWithEmailAndPassword(firebaseAuth, email, password);}function saveSignupData(uid) {const newUser = createNewUser(uid, getSignupFormData());const newContact = createNewContact(getNewContact());return Promise.all([newUser, newContact]);}function getSignupFormData() {return {name: document.getElementById('username').value.trim(),email: document.getElementById('email').value.trim(),userColor: getRandomProfileColor()};}function getNewContact() {return {name: document.getElementById('username').value.trim(),email: document.getElementById('email').value.trim(),color: getRandomProfileColor(),phone: ""};}function getSignupErrorMessage(error) {if (error.code === 'auth/email-already-in-use') {return 'An account with this email already exists.';}return 'Signup is currently not available. Please try again.';}async function createNewUser(uid, user) {const response = await fetch(getUserDatabaseUrl(uid), {method: 'PUT',headers: { 'Content-Type': 'application/json' },body: JSON.stringify(user)});return ensureSuccessfulResponse(response, 'User profile could not be created.');}async function createNewContact(contact) {const response = await fetch(getDatabaseUrl('contacts'), {method: 'POST',headers: { 'Content-Type': 'application/json' },body: JSON.stringify(contact)});return ensureSuccessfulResponse(response, 'Contact could not be created.');}function showSignupError(message) {document.getElementById('errorMessage').textContent = message;}function showSignupToast() {const toast = document.querySelector('.signup-toast');if (!toast)return;toast.textContent = 'You signed up successfully';toast.classList.add('show');setTimeout(() => {window.location.href = '../index.html';}, 2000);}

class CollectibleObject extends DrawableObject {worldX = 0;collected = false;animationImages = [];frameDelay = 120;lastFrameChange = 0;type = '';constructor(images, worldX, y, type) {super();this.animationImages = images;this.worldX = worldX;this.y = y;this.type = type;this.loadImages(images);this.reset();}update(cameraX) {this.x = this.worldX - cameraX;if (!this.collected)this.updateAnimation();}updateAnimation() {const now = Date.now();if (now - this.lastFrameChange < this.frameDelay)return;this.playAnimation(this.animationImages);this.lastFrameChange = now;}collect() {this.collected = true;}reset() {this.collected = false;this.currentImage = 0;this.lastFrameChange = 0;this.image = this.imageCache[this.animationImages[0]];}draw(context) {if (!this.collected)super.draw(context);}}

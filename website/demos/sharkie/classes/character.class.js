class Character extends MovableObject {x = 160;worldX = 160;y = 430;width = 270;height = 330;speed = 7;energy = 100;bubbleEnergy = 0;maxBubbleEnergy = 5;lastHit = 0;hurtDuration = 700;damageCooldown = 1200;canvasWidth = 1920;canvasHeight = 1080;levelWidth = 12000;boundaryMargin = 20;keyboard;physics;animator;audioManager;animationInterval = null;movementInterval = null;isActive = false;isDead = false;deathAnimationFinished = false;isJumping = false;verticalVelocity = 0;gravity = 0.4;liftAcceleration = 0.9;diveAcceleration = 0.28;maxRiseSpeed = 8.5;maxSinkSpeed = 4.5;jumpStrength = 10.5;lastActivity = Date.now();sleepDelay = 15000;offset = { top: 155, right: 60, bottom: 80, left: 60 };constructor(keyboard, levelWidth, audioManager) {super();this.keyboard = keyboard;this.levelWidth = levelWidth;this.audioManager = audioManager;this.physics = new CharacterPhysics(this);this.animator = new CharacterAnimator(this, audioManager);this.resetForHome();}reset() {this.stop();this.resetState();this.isActive = true;this.startAnimationLoop();this.startMovementLoop();}resetForHome() {this.stop();this.resetState();}resetState() {this.worldX = 160;this.x = 160;this.y = 430;this.energy = 100;this.bubbleEnergy = 0;this.lastHit = 0;this.lastActivity = Date.now();this.resetTransientState();this.animator.reset();}resetTransientState() {this.currentImage = 0;this.otherDirection = false;this.isDead = false;this.deathAnimationFinished = false;this.resetJump();}startAnimationLoop() {if (this.animationInterval)return;this.animationInterval = setInterval(() => this.animator.update(), 115);}startMovementLoop() {if (this.movementInterval)return;this.movementInterval = setInterval(() => this.move(), 1000 / 60);}move() {if (!this.isActive || this.isDead)return;if (this.keyboard.hasMovementInput())this.registerActivity();this.physics.update();}setObstacles(obstacles) {this.physics.setObstacles(obstacles);}registerActivity() {this.lastActivity = Date.now();}isSleeping() {return !this.keyboard.hasMovementInput() && Date.now() - this.lastActivity >= this.sleepDelay;}startBubbleAttack() {this.registerActivity();this.animator.startBubbleAttack();}startFinSlap() {this.registerActivity();this.animator.startFinSlap();}isFinSlapActive() {return this.animator.isFinSlapActive();}resetJump() {this.isJumping = false;this.verticalVelocity = 0;}hit(damage) {if (this.isDead || !this.canTakeDamage())return false;this.energy = Math.max(0, this.energy - damage);this.lastHit = Date.now();this.currentImage = 0;this.audioManager.playSound('hurt');if (this.energy === 0)this.die();return true;}addBubbleEnergy() {if (this.bubbleEnergy >= this.maxBubbleEnergy)return false;this.bubbleEnergy++;return true;}useBubbleEnergy() {if (this.bubbleEnergy <= 0 || this.isDead)return false;this.bubbleEnergy--;return true;}die() {this.isDead = true;this.isActive = false;this.currentImage = 0;this.resetJump();this.stopMovementLoop();}stop() {this.isActive = false;this.stopMovementLoop();this.stopAnimationLoop();}stopMovementLoop() {clearInterval(this.movementInterval);this.movementInterval = null;}stopAnimationLoop() {clearInterval(this.animationInterval);this.animationInterval = null;}canTakeDamage() {return Date.now() - this.lastHit >= this.damageCooldown;}isHurt() {return Date.now() - this.lastHit < this.hurtDuration;}getMaximumWorldX() {return this.levelWidth - this.width;}getMinimumY() {return this.boundaryMargin - this.offset.top;}getMaximumY() {const visibleHitboxHeight = this.height - this.offset.bottom;return this.canvasHeight - this.boundaryMargin - visibleHitboxHeight;}}

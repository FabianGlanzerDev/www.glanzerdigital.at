class Obstacle extends DrawableObject {worldX = 0;collisionBoxes = [];collisionInset = 7;constructor(imagePath, worldX, y, width, height, collisionBoxes) {super();this.worldX = worldX;this.y = y;this.width = width;this.height = height;this.collisionBoxes = collisionBoxes;this.loadImage(imagePath);}update(cameraX) {this.x = this.worldX - cameraX;}isCollidingWith(object) {return this.collisionBoxes.some((box) => {return this.isScreenBoxColliding(box, object);});}isCollidingAt(object, objectWorldX, objectY, tolerance = 0) {return this.collisionBoxes.some((box) => {return this.isWorldBoxColliding(box, object, objectWorldX, objectY, tolerance);});}isScreenBoxColliding(box, object) {const inset = this.collisionInset;const left = this.x + box.x + inset;const right = this.x + box.x + box.width - inset;const top = this.y + box.y + inset;const bottom = this.y + box.y + box.height - inset;return right > object.getLeftEdge()&& left < object.getRightEdge()&& bottom > object.getTopEdge()&& top < object.getBottomEdge();}isWorldBoxColliding(box, object, objectWorldX, objectY, tolerance) {const inset = this.collisionInset;const left = this.worldX + box.x + inset, right = this.worldX + box.x + box.width - inset;const top = this.y + box.y + inset, bottom = this.y + box.y + box.height - inset;const objectLeft = objectWorldX + object.offset.left + tolerance;const objectRight = objectWorldX + object.width - object.offset.right - tolerance;const objectTop = objectY + object.offset.top + tolerance;const objectBottom = objectY + object.height - object.offset.bottom - tolerance;return right > objectLeft&& left < objectRight&& bottom > objectTop&& top < objectBottom;}}

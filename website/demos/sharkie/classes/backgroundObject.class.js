class BackgroundObject extends DrawableObject {width = 3840;height = 1080;scrollFactor;constructor(imagePath, scrollFactor) {super();this.scrollFactor = scrollFactor;this.loadImage(imagePath);}updatePosition(cameraX) {const travel = cameraX * this.scrollFactor;this.x = -(travel % this.width);}draw(context) {this.drawTile(context, this.x);this.drawTile(context, this.x + this.width);}drawTile(context, x) {context.drawImage(this.image, x, this.y, this.width, this.height);}}

class BubbleProjectile extends MovableObject {width = 82;height = 82;speed = 15;direction = 1;active = true;offset = { top: 10, right: 10, bottom: 10, left: 10 };imagePath = 'assets/images/projectiles/bubble/bubble-projectile.png';constructor(worldX, y, direction) {super();this.worldX = worldX;this.y = y;this.direction = direction;this.loadImage(this.imagePath);}update(cameraX, levelWidth) {if (!this.active)return;this.worldX += this.speed * this.direction;this.updateScreenPosition(cameraX);this.checkBounds(levelWidth, cameraX);}checkBounds(levelWidth, cameraX) {const behindCamera = this.worldX < cameraX - this.width;const aheadOfCamera = this.worldX > cameraX + 2200;if (this.worldX < 0 || this.worldX > levelWidth || behindCamera || aheadOfCamera) {this.active = false;}}deactivate() {this.active = false;}draw(context) {if (!this.active)return;context.save();context.shadowBlur = 24;context.shadowColor = this.getCssVariable('--color-bubble-projectile-glow');super.draw(context);context.restore();}}

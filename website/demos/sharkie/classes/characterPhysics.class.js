class CharacterPhysics {character;obstacles = [];collisionTolerance = 6;constructor(character) {this.character = character;}setObstacles(obstacles) {this.obstacles = obstacles;}update() {this.handleBoost();this.moveHorizontally();this.updateVerticalVelocity();this.applyVerticalMovement();this.updateBoostState();}moveHorizontally() {const character = this.character;if (character.keyboard.right)this.tryMoveWorld(character.speed);if (character.keyboard.left)this.tryMoveWorld(-character.speed);}updateVerticalVelocity() {const character = this.character;character.verticalVelocity += character.gravity;if (character.keyboard.up)character.verticalVelocity -= character.liftAcceleration;if (character.keyboard.down)character.verticalVelocity += character.diveAcceleration;character.verticalVelocity = this.clampVerticalVelocity(character.verticalVelocity);}clampVerticalVelocity(velocity) {const character = this.character;return this.clamp(velocity, -character.maxRiseSpeed, character.maxSinkSpeed);}applyVerticalMovement() {const character = this.character;const moved = this.tryMoveVertical(character.verticalVelocity);if (!moved)character.verticalVelocity = 0;}handleBoost() {if (!this.character.keyboard.consumeJump())return;this.character.registerActivity();this.character.isJumping = true;this.character.verticalVelocity = -this.character.jumpStrength;}updateBoostState() {const character = this.character;if (!character.isJumping || character.verticalVelocity < 0)return;character.isJumping = false;}tryMoveWorld(delta) {const character = this.character;const nextX = this.clamp(character.worldX + delta, 0, character.getMaximumWorldX());character.otherDirection = delta < 0;if (this.canOccupy(nextX, character.y))character.worldX = nextX;}tryMoveVertical(delta) {const character = this.character;const nextY = this.clamp(character.y + delta, character.getMinimumY(), character.getMaximumY());if (Math.abs(nextY - character.y) < 0.01)return false;if (!this.canOccupy(character.worldX, nextY))return false;character.y = nextY;return true;}canOccupy(worldX, y) {return !this.obstacles.some((obstacle) => {return obstacle.isCollidingAt(this.character, worldX, y, this.collisionTolerance);});}clamp(value, minimum, maximum) {return Math.min(Math.max(value, minimum), maximum);}}

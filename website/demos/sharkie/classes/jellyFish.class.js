class JellyFish extends MovableObject {width = 150;height = 214;damage = 25;patrolWidth = 230;patrolHeight = 300;movementAngle = 0;movementSpeed = 0.018;currentCameraX = 0;animationInterval = null;movementInterval = null;isActive = false;isDead = false;isRemoved = false;hurtUntil = 0;hurtDuration = 600;deadUntil = 0;deadDuration = 900;initialWorldX;initialY;patrolCenterX;patrolCenterY;offset = { top: 30, right: 30, bottom: 26, left: 30 };swimmingImages = ['assets/images/enemies/jelly-fish/swim/jelly-fish-swim-01.png','assets/images/enemies/jelly-fish/swim/jelly-fish-swim-02.png','assets/images/enemies/jelly-fish/swim/jelly-fish-swim-03.png','assets/images/enemies/jelly-fish/swim/jelly-fish-swim-04.png'];hurtImages = ['assets/images/enemies/jelly-fish/hurt/jelly-fish-hurt-01.png','assets/images/enemies/jelly-fish/hurt/jelly-fish-hurt-02.png','assets/images/enemies/jelly-fish/hurt/jelly-fish-hurt-03.png','assets/images/enemies/jelly-fish/hurt/jelly-fish-hurt-04.png'];deadImages = ['assets/images/enemies/jelly-fish/dead/jelly-fish-dead-01.png','assets/images/enemies/jelly-fish/dead/jelly-fish-dead-02.png','assets/images/enemies/jelly-fish/dead/jelly-fish-dead-03.png','assets/images/enemies/jelly-fish/dead/jelly-fish-dead-04.png'];constructor(worldX, y) {super();this.initialWorldX = worldX;this.initialY = y;this.loadImages(this.swimmingImages);this.loadImages(this.hurtImages);this.loadImages(this.deadImages);this.resetForHome();}start(cameraX = 0) {this.stop();this.currentCameraX = cameraX;this.resetState();this.isActive = true;this.startAnimationLoop();this.startMovementLoop();}resetForHome() {this.stop();this.currentCameraX = 0;this.resetState();}resetState() {this.isDead = false;this.isRemoved = false;this.hurtUntil = 0;this.deadUntil = 0;this.currentImage = 0;this.movementAngle = 0;this.patrolCenterX = this.initialWorldX;this.patrolCenterY = this.initialY;this.image = this.imageCache[this.swimmingImages[0]];this.updatePatrolPosition();}startAnimationLoop() {this.animationInterval = setInterval(() => {if (this.isRemoved)return;this.playAnimation(this.getAnimationImages());}, 150);}getAnimationImages() {if (Date.now() < this.hurtUntil)return this.hurtImages;return this.isDead ? this.deadImages : this.swimmingImages;}startMovementLoop() {this.movementInterval = setInterval(() => this.move(), 1000 / 60);}move() {if (!this.isActive || this.isDead || this.isRemoved)return;this.movementAngle += this.movementSpeed;this.updatePatrolPosition();}updatePatrolPosition() {this.worldX = this.patrolCenterX + Math.sin(this.movementAngle * 0.7) * this.patrolWidth;this.y = this.patrolCenterY + Math.sin(this.movementAngle) * this.patrolHeight / 2;this.updateScreenPosition(this.currentCameraX);}update(cameraX) {this.currentCameraX = cameraX;this.finishDefeat();this.updateScreenPosition(cameraX);}takeHit() {if (this.isDead || this.isRemoved || !this.isActive)return false;const now = Date.now();this.isDead = true;this.hurtUntil = now + this.hurtDuration;this.deadUntil = this.hurtUntil + this.deadDuration;this.currentImage = 0;this.image = this.imageCache[this.hurtImages[0]];return true;}finishDefeat() {if (!this.isDead || Date.now() < this.deadUntil)return;this.isRemoved = true;this.stop();}stop() {this.isActive = false;clearInterval(this.animationInterval);clearInterval(this.movementInterval);this.animationInterval = null;this.movementInterval = null;}draw(context) {if (!this.isRemoved)super.draw(context);}}

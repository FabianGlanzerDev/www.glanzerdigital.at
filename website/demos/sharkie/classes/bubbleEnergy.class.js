const bubbleEnergyImages = ['assets/images/collectibles/bubble-energy/bubble-energy.png'];class BubbleEnergy extends CollectibleObject {baseSize = 92;width = 92;height = 92;offset = { top: 10, right: 10, bottom: 10, left: 10 };pulseOffset = Math.random() * Math.PI * 2;constructor(worldX, y) {super(bubbleEnergyImages, worldX, y, 'bubble-energy');}update(cameraX) {super.update(cameraX);this.applyPulse();}applyPulse() {const pulse = Math.sin(Date.now() / 220 + this.pulseOffset) * 0.07;this.width = this.baseSize * (1 + pulse);this.height = this.baseSize * (1 + pulse);}draw(context) {if (this.collected)return;context.save();context.shadowBlur = 30;context.shadowColor = this.getCssVariable('--color-bubble-energy-glow');super.draw(context);context.restore();}}

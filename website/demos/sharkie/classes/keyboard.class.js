class Keyboard {left = false;right = false;up = false;down = false;space = false;jumpRequested = false;attack = false;attackRequested = false;slap = false;slapRequested = false;keyMap = {ArrowLeft: 'left', KeyA: 'left', ArrowRight: 'right', KeyD: 'right',ArrowUp: 'up', KeyW: 'up', ArrowDown: 'down', KeyS: 'down',Space: 'jump', KeyX: 'attack', KeyE: 'attack', KeyY: 'slap', KeyF: 'slap'};constructor() {this.bindEvents();}bindEvents() {window.addEventListener('keydown', (event) => this.handleKeyEvent(event, true));window.addEventListener('keyup', (event) => this.handleKeyEvent(event, false));}handleKeyEvent(event, isPressed) {const action = this.keyMap[event.code];if (!action)return;event.preventDefault();this.setControl(action, isPressed);}setControl(action, isPressed) {if (action === 'jump')return this.updateJump(isPressed);if (action === 'attack')return this.updateAttack(isPressed);if (action === 'slap')return this.updateSlap(isPressed);if (action in this)this[action] = isPressed;}updateJump(isPressed) {if (isPressed && !this.space)this.jumpRequested = true;this.space = isPressed;}updateAttack(isPressed) {if (isPressed && !this.attack)this.attackRequested = true;this.attack = isPressed;}updateSlap(isPressed) {if (isPressed && !this.slap)this.slapRequested = true;this.slap = isPressed;}consumeJump() {const requested = this.jumpRequested;this.jumpRequested = false;return requested;}consumeAttack() {const requested = this.attackRequested;this.attackRequested = false;return requested;}consumeSlap() {const requested = this.slapRequested;this.slapRequested = false;return requested;}hasMovementInput() {return this.left || this.right || this.up || this.down;}reset() {['left', 'right', 'up', 'down', 'space', 'attack', 'slap'].forEach((key) => this[key] = false);this.jumpRequested = false;this.attackRequested = false;this.slapRequested = false;}}

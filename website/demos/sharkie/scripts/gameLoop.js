function drawGame() {context.clearRect(0, 0, canvas.width, canvas.height);updateGame();drawGameObjects();requestAnimationFrame(drawGame);}function updateGame() {if (gameState !== 'playing')return;const cameraX = getCameraX();updateBackground(cameraX);updateWorldPositions(cameraX);updateGameplayInteractions(cameraX);}function updateGameplayInteractions(cameraX) {handleCombatInput(cameraX);updateProjectiles(cameraX);checkPlayerCollisions();checkCollectibleCollisions();checkWinCondition();}function drawGameObjects() {drawBackground();if (isGameSceneVisible())drawWorldObjects();lightLayer.draw(context);if (isGameSceneVisible())drawStatusDisplays();}function drawWorldObjects() {drawAmbientLife();drawObstacles();drawCollectibles();drawEnemies();endboss.draw(context);drawProjectiles();sharkie.draw(context);}function updateBackground(cameraX) {backgroundLayers.forEach((layer) => layer.updatePosition(cameraX));lightLayer.updatePosition(cameraX);}function updateWorldPositions(cameraX) {sharkie.updateScreenPosition(cameraX);updateAmbientLife(cameraX);updateObstacles(cameraX);updateInteractiveObjects(cameraX);endboss.update(cameraX, sharkie.worldX, sharkie.y);}function updateInteractiveObjects(cameraX) {collectibles.forEach((item) => item.update(cameraX));enemies.forEach((enemy) => enemy.update(cameraX));}function drawBackground() {backgroundLayers.forEach((layer) => layer.draw(context));}function drawCollectibles() {collectibles.forEach((item) => item.draw(context));}function drawEnemies() {enemies.forEach((enemy) => enemy.draw(context));}function drawStatusDisplays() {healthBar.draw(context);coinCounter.draw(context);bubbleEnergyCounter.draw(context);if (endboss.isActive && !endboss.isDefeated)bossStatusBar.draw(context);}function getCameraX() {const desiredX = sharkie.worldX - level.cameraAnchorX;const maximumX = level.width - canvas.width;return Math.min(Math.max(0, desiredX), maximumX);}

function initializeGame() {bindInterfaceEvents();bindMobileControls();showHomeScreen();drawGame();}function startGame() {if (!canStartGame())return;prepareNewRound();startRoundActors();resetRoundObjects();audioManager.startMusic();releaseInterfaceFocus();updateScreenVisibility();}function prepareNewRound() {clearTransitionTimers();gameState = 'playing';keyboard.reset();sharkie.reset();}function startRoundActors() {enemies.forEach((enemy) => enemy.start(0));endboss.start(0);startAmbientLife(0);}function canStartGame() {return ['controls', 'gameover', 'win'].includes(gameState);}function resetRoundObjects() {resetCollectibles();clearProjectiles();resetStatusDisplays();updateWorldPositions(0);updateBackground(0);}function showHomeScreen() {if (!canShowHomeScreen())return;prepareHomeState();resetHomeActors();resetRoundObjects();audioManager.stopAll();updateScreenVisibility();}function prepareHomeState() {clearTransitionTimers();gameState = 'home';keyboard.reset();sharkie.resetForHome();}function resetHomeActors() {enemies.forEach((enemy) => enemy.resetForHome());endboss.resetForHome();resetAmbientLife();}function canShowHomeScreen() {return ['home', 'controls', 'gameover', 'win'].includes(gameState);}function startGameOverSequence() {if (gameState !== 'playing')return;gameState = 'dying';stopEnemiesAndProjectiles();audioManager.stopMusic();audioManager.playSound('gameOver');updateScreenVisibility();gameOverTimer = setTimeout(showGameOverScreen, 1650);}function startWinSequence() {if (gameState !== 'playing')return;gameState = 'winning';stopActiveGameplay();audioManager.stopMusic();audioManager.playSound('win');updateScreenVisibility();winTimer = setTimeout(showWinScreen, 1300);}function stopActiveGameplay() {keyboard.reset();sharkie.stop();stopEnemiesAndProjectiles();}function stopEnemiesAndProjectiles() {keyboard.reset();enemies.forEach((enemy) => enemy.stop());endboss.stop();stopAmbientLife();clearProjectiles();}function checkWinCondition() {if (endboss.isDefeated)startWinSequence();}function clearTransitionTimers() {clearTimeout(gameOverTimer);clearTimeout(winTimer);gameOverTimer = null;winTimer = null;}

const mobileJoystickZone = document.getElementById('mobileJoystickZone');const mobileJoystick = document.getElementById('mobileJoystick');const mobileJoystickKnob = document.getElementById('mobileJoystickKnob');const mobileJoystickState = {pointerId: null,centerX: 0,centerY: 0,radius: 0};function bindMobileControls() {const buttons = mobileControls.querySelectorAll('.mobile-actions [data-control]');buttons.forEach((button) => bindMobileControlButton(button));bindMobileJoystick();mobileControls.addEventListener('contextmenu', preventMobileContextMenu);window.addEventListener('blur', releaseMobileControls);document.addEventListener('visibilitychange', releaseHiddenMobileControls);}function bindMobileJoystick() {mobileJoystickZone.addEventListener('pointerdown', startMobileJoystick);mobileJoystickZone.addEventListener('pointermove', moveMobileJoystick);mobileJoystickZone.addEventListener('pointerup', stopMobileJoystick);mobileJoystickZone.addEventListener('pointercancel', stopMobileJoystick);}function startMobileJoystick(event) {if (mobileJoystickState.pointerId !== null)return;event.preventDefault();mobileJoystickState.pointerId = event.pointerId;mobileJoystickZone.setPointerCapture?.(event.pointerId);setMobileJoystickCenter();mobileJoystick.classList.add('is-active');updateMobileJoystick(event);}function setMobileJoystickCenter() {const joystickRect = mobileJoystick.getBoundingClientRect();mobileJoystickState.centerX = joystickRect.left + joystickRect.width / 2;mobileJoystickState.centerY = joystickRect.top + joystickRect.height / 2;mobileJoystickState.radius = joystickRect.width * 0.28;}function moveMobileJoystick(event) {if (event.pointerId !== mobileJoystickState.pointerId)return;event.preventDefault();updateMobileJoystick(event);}function updateMobileJoystick(event) {const vector = getLimitedJoystickVector(event);updateMobileJoystickVisual(vector);updateJoystickMovement(vector);}function updateMobileJoystickVisual(vector) {const x = `calc(-50% + ${vector.x}px)`;const y = `calc(-50% + ${vector.y}px)`;mobileJoystickKnob.style.transform = `translate(${x}, ${y})`;}function getLimitedJoystickVector(event) {const x = event.clientX - mobileJoystickState.centerX;const y = event.clientY - mobileJoystickState.centerY;const distance = Math.hypot(x, y) || 1;const scale = Math.min(1, mobileJoystickState.radius / distance);return { x: x * scale, y: y * scale };}function updateJoystickMovement(vector) {const deadZone = mobileJoystickState.radius * 0.28;keyboard.setControl('left', vector.x < -deadZone);keyboard.setControl('right', vector.x > deadZone);keyboard.setControl('up', vector.y < -deadZone);keyboard.setControl('down', vector.y > deadZone);}function stopMobileJoystick(event) {if (event.pointerId !== mobileJoystickState.pointerId)return;event.preventDefault();mobileJoystickState.pointerId = null;releaseJoystickMovement();resetMobileJoystickVisual();}function releaseJoystickMovement() {['left', 'right', 'up', 'down'].forEach((control) => {keyboard.setControl(control, false);});}function resetMobileJoystickVisual() {mobileJoystick.classList.remove('is-active');mobileJoystickKnob.style.transform = '';}function bindMobileControlButton(button) {button.addEventListener('pointerdown', handleMobilePointerDown);button.addEventListener('pointerup', handleMobilePointerUp);button.addEventListener('pointercancel', handleMobilePointerUp);}function handleMobilePointerDown(event) {event.preventDefault();event.currentTarget.setPointerCapture?.(event.pointerId);updateMobileControl(event.currentTarget, true);}function handleMobilePointerUp(event) {event.preventDefault();updateMobileControl(event.currentTarget, false);}function updateMobileControl(button, isPressed) {const action = button.dataset.control;keyboard.setControl(action, isPressed);}function releaseMobileControls() {mobileJoystickState.pointerId = null;keyboard.reset();resetMobileJoystickVisual();}function releaseHiddenMobileControls() {if (document.hidden)releaseMobileControls();}function preventMobileContextMenu(event) {event.preventDefault();}

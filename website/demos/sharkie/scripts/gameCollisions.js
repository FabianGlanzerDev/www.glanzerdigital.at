function checkPlayerCollisions() {enemies.forEach((enemy) => handleEnemyCollision(enemy));handleBossCollision();}function handleEnemyCollision(enemy) {if (enemy.isDead || enemy.isRemoved || !sharkie.isColliding(enemy, 5))return;if (!sharkie.hit(enemy.damage))return;healthBar.setPercentage(sharkie.energy);applyKnockback(enemy.worldX);if (sharkie.isDead)startGameOverSequence();}function handleBossCollision() {if (!endboss.isActive || endboss.isDead || !sharkie.isColliding(endboss, 6))return;if (!sharkie.hit(endboss.damage))return;healthBar.setPercentage(sharkie.energy);applyKnockback(endboss.worldX);if (sharkie.isDead)startGameOverSequence();}function applyKnockback(sourceWorldX) {const direction = sourceWorldX > sharkie.worldX ? -1 : 1;sharkie.physics.tryMoveWorld(direction * 55);}function checkCollectibleCollisions() {collectibles.forEach((item) => handleCollectibleCollision(item));}function handleCollectibleCollision(item) {if (item.collected || !sharkie.isColliding(item, 2))return;if (item.type === 'bubble-energy' && !sharkie.addBubbleEnergy())return;item.collect();registerCollectiblePickup();}function registerCollectiblePickup() {sharkie.registerActivity();audioManager.playSound('collect');updateCollectibleCounters();}function resetCollectibles() {collectibles.forEach((item) => item.reset());collectibles.forEach((item) => item.update(0));}function resetStatusDisplays() {healthBar.setPercentage(100);bossStatusBar.setPercentage(100);coinCounter.setValue(0);bubbleEnergyCounter.setValue(0);}function updateCollectibleCounters() {coinCounter.setValue(countCollectedItems(coins));bubbleEnergyCounter.setValue(sharkie.bubbleEnergy);}function countCollectedItems(items) {return items.filter((item) => item.collected).length;}

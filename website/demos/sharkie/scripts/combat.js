function handleCombatInput(cameraX) {handleBubbleAttack(cameraX);handleFinSlapAttack();}function handleBubbleAttack(cameraX) {if (!keyboard.consumeAttack())return;sharkie.registerActivity();if (!sharkie.useBubbleEnergy())return;sharkie.startBubbleAttack();projectiles.push(createBubbleProjectile(cameraX));bubbleEnergyCounter.setValue(sharkie.bubbleEnergy);audioManager.playSound('bubble');}function createBubbleProjectile(cameraX) {const direction = sharkie.otherDirection ? -1 : 1;const projectile = new BubbleProjectile(getBubbleStartX(direction), getBubbleStartY(), direction);projectile.updateScreenPosition(cameraX);return projectile;}function getBubbleStartX(direction) {return direction < 0 ? sharkie.worldX + 30 : sharkie.worldX + sharkie.width - 75;}function getBubbleStartY() {return sharkie.y + sharkie.height * 0.46;}function handleFinSlapAttack() {if (!keyboard.consumeSlap())return;sharkie.startFinSlap();audioManager.playSound('finSlap');enemies.forEach((enemy) => applyFinSlapToEnemy(enemy));applyFinSlapToBoss();}function applyFinSlapToEnemy(enemy) {if (!(enemy instanceof JellyFish) || enemy.isDead || enemy.isRemoved)return;if (!isInsideFinSlapRange(enemy))return;if (enemy.takeHit())audioManager.playSound('enemyHit');}function applyFinSlapToBoss() {if (!endboss.isActive || endboss.isDead)return;if (!isInsideFinSlapRange(endboss))return;if (!endboss.takeHit())return;bossStatusBar.setPercentage(endboss.health);audioManager.playSound('bossHit');}function isInsideFinSlapRange(object) {const reach = 95;const left = sharkie.getLeftEdge() - reach;const right = sharkie.getRightEdge() + reach;return right > object.getLeftEdge() && left < object.getRightEdge()&& sharkie.getBottomEdge() > object.getTopEdge()&& sharkie.getTopEdge() < object.getBottomEdge();}function updateProjectiles(cameraX) {projectiles.forEach((projectile) => projectile.update(cameraX, level.width));checkProjectileCollisions();removeInactiveProjectiles();}function checkProjectileCollisions() {projectiles.forEach((projectile) => {enemies.forEach((enemy) => handleProjectileEnemyHit(projectile, enemy));handleProjectileBossHit(projectile);});}function handleProjectileEnemyHit(projectile, enemy) {if (!projectile.active || enemy.isDead || enemy.isRemoved)return;if (!projectile.isColliding(enemy, 2))return;projectile.deactivate();if (enemy.takeHit())audioManager.playSound('enemyHit');}function handleProjectileBossHit(projectile) {if (!projectile.active || !endboss.isActive || endboss.isDead)return;if (!projectile.isColliding(endboss, 4))return;if (!endboss.takeHit())return;projectile.deactivate();bossStatusBar.setPercentage(endboss.health);audioManager.playSound('bossHit');}function removeInactiveProjectiles() {projectiles = projectiles.filter((projectile) => projectile.active);}function drawProjectiles() {projectiles.forEach((projectile) => projectile.draw(context));}function clearProjectiles() {projectiles = [];}
